{% extends 'base.html' %}

{% block head %}
<title>Task Master</title>
{% endblock %}

{% block body %}
<div class="content">
    <h1 style="text-align: center">Park Finder</h1>
    {% if tasks|length < 1 %}
    <h4 style="text-align: center">There are no parkings. Create one below!</h4>
    {% else %}
    <table>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Added</th>
            <th>Time</th>
            <th>Booking Status</th>
            <th>Location</th>
        </tr>
        {% for task in tasks %}
            <tr>
                <td>{{ task.id }}</td>
                <td>{{ task.content }}</td>
                <td>{{ task.date_created.date() }}</td>
                <td></td>
                <td>
                    <a href="/update/{{task.id}}">Update</a>
                    <a href="/delete/{{task.id}}">Delete</a>
                </td>
                <td></td>
            </tr>
        {% endfor %}
    </table>
    {% endif %}

    <div class="form">
        <form action="/" method="POST">
            <input type="text" name="content">
            
            <input type="submit" value="Add Task">
        </form>
    </div>
</div>
{% endblock %}
